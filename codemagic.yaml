# codemagic.yaml
# CI configuration for building MetallumShaders on CodeMagic.
# Built for macOS ARM64 (Apple Silicon).
# Minecraft 26.2 / Java 25 / Gradle 9.6.1 / Fabric Loom 1.17.17 (Kotlin DSL)

workflows:
  metallum-shaders-build:
    name: Metallum Shaders Build
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      vars:
        JAVA_VERSION: "25"
      xcode: latest
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: main
          include: true
        - pattern: develop
          include: true
      cancel_previous_builds: true
    cache:
      cache_paths:
        - ~/.gradle/caches
        - ~/.gradle/wrapper
        - .gradle/
    scripts:
      - name: Install Java 25 (Adoptium)
        script: |
          #!/usr/bin/env bash
          set -e
          echo "=== Installing Java 25 (Adoptium) ==="
          brew tap homebrew/cask-versions 2>/dev/null || true
          brew install --cask temurin@25 2>/dev/null || true
          export JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-25.jdk/Contents/Home
          sudo ln -sf $JAVA_HOME/bin/java /usr/local/bin/java
          sudo ln -sf $JAVA_HOME/bin/javac /usr/local/bin/javac
          echo "JAVA_HOME=$JAVA_HOME" >> $CM_ENV
          echo "PATH=$JAVA_HOME/bin:$PATH" >> $CM_ENV
          java -version

      - name: Download dependencies (Others)
        script: |
          #!/usr/bin/env bash
          set -e
          mkdir -p libs
          
          # Metallum
          if [ ! -f libs/metallum-1.0.1.jar ]; then
            echo "Downloading Metallum jar..."
            curl -L -o libs/metallum-1.0.1.jar \
              "https://github.com/EternityQwQ/MetalUniversal/releases/download/MetalUniversal_1.0.1/metallum-1.0.1.jar" \
              || echo "WARNING: could not download Metallum jar"
            if [ ! -s libs/metallum-1.0.1.jar ]; then
              mkdir -p tmp-stub/META-INF
              printf "Manifest-Version: 1.0\n" > tmp-stub/META-INF/MANIFEST.MF
              (cd tmp-stub && jar cf ../libs/metallum-1.0.1.jar META-INF/MANIFEST.MF)
              rm -rf tmp-stub
            fi
          fi
          
          # Sodium
          if [ ! -f libs/sodium-fabric-0.9.1+mc26.2.jar ]; then
            echo "Downloading Sodium jar..."
            curl -L -o libs/sodium-fabric-0.9.1+mc26.2.jar \
              "https://cdn.modrinth.com/data/AANobbMI/versions/2Yom1N68/sodium-fabric-0.9.1%2Bmc26.2.jar" \
              || echo "WARNING: could not download Sodium jar"
            if [ ! -s libs/sodium-fabric-0.9.1+mc26.2.jar ]; then
              mkdir -p tmp-stub/META-INF
              printf "Manifest-Version: 1.0\n" > tmp-stub/META-INF/MANIFEST.MF
              (cd tmp-stub && jar cf ../libs/sodium-fabric-0.9.1+mc26.2.jar META-INF/MANIFEST.MF)
              rm -rf tmp-stub
            fi
          fi

      - name: Ensure Minecraft client JAR
        script: |
          #!/usr/bin/env bash
          set -e
          mkdir -p libs
          JAR_FILE="libs/minecraft-26.2-client.jar"
          if [ ! -f "$JAR_FILE" ]; then
            echo "Minecraft client jar not found, downloading..."
            # Fetch the download URL from the version manifest
            VERSION_MANIFEST=$(curl -s "https://piston-meta.mojang.com/v1/packages/d98675ecc24364e90b18dbea80390b1345c3f71f/26.2.json")
            CLIENT_URL=$(echo "$VERSION_MANIFEST" | jq -r '.downloads.client.url')
            if [ -z "$CLIENT_URL" ] || [ "$CLIENT_URL" == "null" ]; then
              echo "ERROR: Could not extract client URL from version manifest"
              exit 1
            fi
            curl -sL "$CLIENT_URL" -o "$JAR_FILE"
            echo "Downloaded Minecraft client jar to $JAR_FILE"
          else
            echo "Minecraft client jar already exists at $JAR_FILE"
          fi

      - name: Setup local Maven repository for Minecraft
        script: |
          #!/usr/bin/env bash
          set -e
          LOCAL_REPO="maven_repo"
          mkdir -p "${LOCAL_REPO}/com/mojang/minecraft/26.2"
          cp libs/minecraft-26.2-client.jar "${LOCAL_REPO}/com/mojang/minecraft/26.2/minecraft-26.2.jar"
          # Create a minimal POM so Loom sees this as a valid dependency
          cat > "${LOCAL_REPO}/com/mojang/minecraft/26.2/minecraft-26.2.pom" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
                   xmlns="http://maven.apache.org/POM/4.0.0"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
              <modelVersion>4.0.0</modelVersion>
              <groupId>com.mojang</groupId>
              <artifactId>minecraft</artifactId>
              <version>26.2</version>
              <packaging>jar</packaging>
          </project>
          EOF
          echo "Local Maven repository prepared with POM."

      - name: Setup Gradle Wrapper
        script: |
          #!/usr/bin/env bash
          set -e
          export JAVA_HOME=${JAVA_HOME:-/Library/Java/JavaVirtualMachines/temurin-25.jdk/Contents/Home}
          export PATH=$JAVA_HOME/bin:$PATH
          mkdir -p gradle/wrapper
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "Downloading gradle-wrapper.jar..."
            curl -L -o gradle/wrapper/gradle-wrapper.jar \
              "https://github.com/gradle/gradle/raw/v9.6.1/gradle/wrapper/gradle-wrapper.jar" \
              || {
                if command -v gradle &>/dev/null; then
                  gradle wrapper --gradle-version 9.6.1 --distribution-type bin
                else
                  echo "ERROR: gradle-wrapper.jar download failed and gradle not found"
                  exit 1
                fi
              }
          fi
          chmod +x ./gradlew

      - name: Build native Metal JNI shim (ARM64)
        script: |
          #!/usr/bin/env bash
          set -e
          export JAVA_HOME=${JAVA_HOME:-/Library/Java/JavaVirtualMachines/temurin-25.jdk/Contents/Home}
          echo "Building native Metal JNI shim for ARM64"
          mkdir -p build/native/macos-arm64
          SRC=src/main/cpp/metallum_shaders.cpp
          SDK=$(xcrun --sdk macosx --show-sdk-path)
          clang++ -x objective-c++ -std=c++17 -stdlib=libc++ -fobjc-arc \
            -arch arm64 -dynamiclib \
            -I"$SDK/System/Library/Frameworks/Metal.framework/Headers" \
            -I"$SDK/System/Library/Frameworks/Foundation.framework/Headers" \
            -I"$JAVA_HOME/include" \
            -I"$JAVA_HOME/include/darwin" \
            -F"$SDK/System/Library/Frameworks" \
            -framework Metal -framework Foundation \
            -o build/native/macos-arm64/libmetallum_shaders.dylib "$SRC"
          mkdir -p src/main/resources/native/macos-arm64
          cp build/native/macos-arm64/libmetallum_shaders.dylib \
            src/main/resources/native/macos-arm64/
          echo "Native arm64 dylib built and bundled."
          echo "=== Verify Minecraft jar ==="
          ls -lh libs/minecraft-26.2-client.jar || exit 1
          jar tf libs/minecraft-26.2-client.jar | grep "net/minecraft/client/MinecraftClient.class" || {
             echo "Minecraft jar does not contain MinecraftClient.class - invalid or corrupted jar"
             exit 1
          }
         echo "Minecraft jar verified."
      - name: Build mod jar
        script: |
          #!/usr/bin/env bash
          set -e
          export JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-25.jdk/Contents/Home
          export PATH=$JAVA_HOME/bin:$PATH
          echo "=== Environment Check ==="
          java -version
          echo "Stopping all Gradle daemons..."
          ./gradlew --stop || true
          
          echo "Building mod jar with Java 25 and Gradle wrapper..."
          ./gradlew clean build --no-daemon --stacktrace \
            -Dorg.gradle.java.home=$JAVA_HOME \
            -Dorg.gradle.jvmargs=-Xmx4g

      - name: Verify jar contents
        script: |
          #!/usr/bin/env bash
          set -e
          JAR=$(ls build/libs/metallum-shaders-*.jar | head -n1)
          echo "Built jar: $JAR"
          jar tf "$JAR" | head -50
          echo "----"
          echo "Shader files:"
          jar tf "$JAR" | grep -E '\.(metal|metalh)$' || true
          echo "----"
          echo "Native libs:"
          jar tf "$JAR" | grep -E '\.dylib$' || echo "(no dylibs found)"

    artifacts:
      - build/libs/*.jar
      - build/native/**/*.dylib
    publishing:
      email:
        recipients:
          - user@example.com
        notify:
          success: true
          failure: true
