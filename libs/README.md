# libs/

Drop the Metallum jar from
https://github.com/EternityQwQ/MetalUniversal/releases
into this directory. The build expects it to be named
`metallum-<version>.jar` (e.g. `metallum-0.1.0.jar`).

The version is controlled by `metallum_version` in `gradle.properties`.
